import { SecondaryButtonDirective } from './secondary-button.directive';

describe('SecondaryButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new SecondaryButtonDirective();
    expect(directive).toBeTruthy();
  });
});
