import { ThemeVariantPipe } from './theme-variant.pipe';

describe('ThemeVariantPipe', () => {
  it('create an instance', () => {
    const pipe = new ThemeVariantPipe();
    expect(pipe).toBeTruthy();
  });
});
