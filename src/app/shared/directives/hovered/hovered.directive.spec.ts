import { HoveredDirective } from './hovered.directive';

describe('HoveredDirective', () => {
  it('should create an instance', () => {
    const directive = new HoveredDirective();
    expect(directive).toBeTruthy();
  });
});
