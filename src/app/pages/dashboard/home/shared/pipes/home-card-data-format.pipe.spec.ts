import { HomeCardDataFormatPipe } from './home-card-data-format.pipe';

describe('HomeCardDataFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new HomeCardDataFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
